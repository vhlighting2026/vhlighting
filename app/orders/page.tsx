"use client";import{useEffect,useMemo,useState}from"react";import Link from"next/link";import AuthGuard from"@/components/AuthGuard";import AppShell from"@/components/AppShell";import{supabase}from"@/lib/supabase";import type{Order}from"@/lib/types";import{Plus,Search,ShoppingCart,Wallet}from"lucide-react";
export default function Page(){const[d,setD]=useState<Order[]>([]);const[q,setQ]=useState('');useEffect(()=>{supabase.from('orders').select('*').order('created_at',{ascending:false}).then(({data})=>setD(data||[]))},[]);const filtered=useMemo(()=>d.filter(x=>(x.order_no+' '+x.customer_name+' '+(x.customer_phone||'')).toLowerCase().includes(q.toLowerCase())),[d,q]);return <AuthGuard><AppShell title="Đơn hàng" subtitle="Tra cứu, theo dõi và in phiếu bán hàng" action={<Link className="primary inline" href="/orders/new"><Plus size={18}/>Tạo đơn</Link>}><div className="metric-grid"><article className="metric-card"><div className="metric-icon"><ShoppingCart/></div><div><span>Tổng đơn hàng</span><b>{d.length}</b></div></article><article className="metric-card success"><div className="metric-icon"><Wallet/></div><div><span>Tổng doanh thu</span><b>{d.reduce((a,x)=>a+Number(x.total),0).toLocaleString('vi-VN')} ₫</b></div></article></div><div className="panel"><div className="panel-heading"><div><h2>Danh sách đơn hàng</h2><p>Mở đơn để xem chi tiết hoặc in A4.</p></div><div className="search-field"><Search size={18}/><input placeholder="Tìm số đơn hoặc khách hàng" value={q} onChange={e=>setQ(e.target.value)}/></div></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Số đơn</th><th>Khách hàng</th><th>Ngày tạo</th><th className="align-right">Tổng tiền</th><th>Trạng thái</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td><Link className="order-link" href={'/orders/'+x.id}>{x.order_no}</Link></td><td><b>{x.customer_name}</b><small>{x.customer_phone||''}</small></td><td>{new Date(x.created_at).toLocaleString('vi-VN')}</td><td className="align-right"><b>{Number(x.total).toLocaleString('vi-VN')} ₫</b></td><td><span className="status info-status">{x.status}</span></td></tr>)}{!filtered.length&&<tr><td colSpan={5} className="empty-state">Chưa có đơn hàng phù hợp.</td></tr>}</tbody></table></div></div></AppShell></AuthGuard>}
