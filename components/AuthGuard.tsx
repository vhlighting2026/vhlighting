"use client";
import {useEffect,useState} from "react"; import {useRouter} from "next/navigation"; import {supabase} from "@/lib/supabase";
export default function AuthGuard({children}:{children:React.ReactNode}){const r=useRouter();const[ok,setOk]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>{if(!data.session)r.replace('/login');else setOk(true)});const{data:s}=supabase.auth.onAuthStateChange((_e,session)=>{if(!session)r.replace('/login')});return()=>s.subscription.unsubscribe()},[r]);return ok?<>{children}</>:<div className="center">Đang kiểm tra đăng nhập…</div>}